import 'package:flutter/material.dart';

class seblog4 extends StatelessWidget {
  const seblog4({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Blog4"),
      ),
    );
  }
}
