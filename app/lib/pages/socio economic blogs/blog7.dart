import 'package:flutter/material.dart';

class seblog7 extends StatelessWidget {
  const seblog7({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Blog7"),
      ),
    );
  }
}
