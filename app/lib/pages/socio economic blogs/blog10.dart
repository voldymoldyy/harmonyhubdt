import 'package:flutter/material.dart';

class seblog10 extends StatelessWidget {
  const seblog10({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Blog10"),
      ),
    );
  }
}
