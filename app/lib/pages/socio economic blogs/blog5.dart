import 'package:flutter/material.dart';

class seblog5 extends StatelessWidget {
  const seblog5({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Blog5"),
      ),
    );
  }
}
