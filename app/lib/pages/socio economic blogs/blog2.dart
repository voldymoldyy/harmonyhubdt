import 'package:flutter/material.dart';

class seblog2 extends StatelessWidget {
  const seblog2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Blog2"),
      ),
    );
  }
}
