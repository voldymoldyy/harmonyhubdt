import 'package:flutter/material.dart';

class seblog8 extends StatelessWidget {
  const seblog8({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Blog8"),
      ),
    );
  }
}
