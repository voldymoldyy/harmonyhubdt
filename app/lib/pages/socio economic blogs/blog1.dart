import 'package:flutter/material.dart';

class seblog1 extends StatelessWidget {
  const seblog1({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Blog1"),
      ),
    );
  }
}
