import 'package:flutter/material.dart';

class seblog9 extends StatelessWidget {
  const seblog9({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Blog9"),
      ),
    );
  }
}
